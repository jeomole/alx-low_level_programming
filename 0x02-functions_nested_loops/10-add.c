#include "main.h"

/**
 * add - returns the sum of its parameters
 * @a: Integer to be added
 * @b: Integer to be added
 * @c : Sum of the numbers
 * Return: Returns 0
 */
int add(int a, int b)
{
	int c = (a + b);

	_putchar('0' + c);
	return (c);
}
