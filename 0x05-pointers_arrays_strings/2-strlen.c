#include "main.h"

/**
  * _strlen - returns the length of string
  * @s - string to be evaluated
  * Return: length of string
  */
int _strlen(char *s)
{
	int i = 0;

	for (; s[i] != '\0'; i++)
		return (i);
}
